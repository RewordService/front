import {createMuiTheme} from "@material-ui/core/styles"
const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#757ce8",
      main: "#79bac1",
      dark: "#2a7886",
      contrastText: "#fff",
    },
  },
})

export default theme
